import { Logo } from "@/app/assets/Logo";

export function Footer() {
  return (
    <footer className="bg-white px-8 flex items-center justify-between border-t-2 border-zinc-500">
      <Logo />
    </footer>
  );
}
